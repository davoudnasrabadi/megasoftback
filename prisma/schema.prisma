datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Category {
  id        Int    @id @unique @default(autoincrement())
  latitude  Int
  longitude Int
  category  String
  counter   Int
}
